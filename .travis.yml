before_install:
  - gem install bundler
language: ruby
cache: bundler
rvm:
  - 2.4.1
  - 2.3.4
  - 2.2.7
  - ruby-head
  - jruby-head
env:
- COVERALLS=true
matrix:
  allow_failures:
    - rvm: ruby-head
    - rvm: jruby-head
